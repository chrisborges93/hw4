<!DOCTYPE html>
<html>
  <head>
    <title>Tacostagram Final</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <link rel="stylesheet" href="/stylesheets/application.css">
  </head>

  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" 
        rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" 
        crossorigin="anonymous">
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js" 
          integrity="sha384-YvpcrYf0tY3lHB60NNkmXc5s9fDVZLESaAA55NDzOxhy9GkcIdslK1eN7N6jIeHz" 
          crossorigin="anonymous"></script>

  <body>
    <nav class="navbar navbar-expand-lg bg-body-tertiary">
      <div class="container-fluid">
        <a class="navbar-brand mb-0 h1" href="/">Tacostagram</a>

        <div class="d-flex align-items-center">
          <% if @current_user %>
            <span class="navbar-text me-3">Logged In As: <strong><%= @current_user["username"] %></strong></span>
            <a href="/logout" class="btn btn-primary me-3">Logout</a>
          <% else %>
            <a href="/users/new" class="btn btn-primary me-3">Signup</a>
            <a href="/login" class="btn btn-primary me-3">Login</a>
          <% end %>
          <a href="/" class="btn btn-primary">Home</a>
        </div>
      </div>
    </nav>

    <div class="container mt-4">
      <p class="text-center text-danger">  
        <%= flash["notice"] %>
      </p>

      <%= yield %>
    </div>
  </body>
</html>
